stages:
  - deploy

deploy-pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/index.html
    - ls -R public
  pages:
    publish: public
  artifacts:
    paths:
      - public/
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'